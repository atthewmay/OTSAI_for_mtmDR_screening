from setuptools import setup, find_packages

setup(
    name="llm_messidor_study",  # Replace with your project name
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here
        # For example: "openai", "pillow", "matplotlib"
    ],
    python_requires=">=3.7",
)

